<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ysi.coronavolunteergwalior">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".registration.SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".donation.DonateActivity"/>
        <activity android:name=".volunterr.SignInActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".volunterr.VolunteerSignUp"/>
        <activity android:name=".donation.ReqestHelp"/>
        <activity android:name=".AboutActivity"/>
        <activity android:name=".volunterr.VolunteerTaskList"/>
    </application>

</manifest>